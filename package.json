{
  "name": "naval-war",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently --names server,client \"npm run dev --workspace=@naval-war/server\" \"npm run dev --workspace=@naval-war/client\"",
    "dev:client": "npm run dev --workspace=@naval-war/client",
    "dev:server": "npm run dev --workspace=@naval-war/server",
    "build": "npm run build --workspace=@naval-war/types && npm run build --workspace=@naval-war/server && npm run build --workspace=@naval-war/client",
    "build:types": "npm run build --workspace=@naval-war/types"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
